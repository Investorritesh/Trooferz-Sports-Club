-- DANGER: local DEMO reset only.
-- This drops and recreates the demo database by re-running the canonical schema and seed.
-- Run from the database folder in a local development environment.
SOURCE schema.sql;
SOURCE seed.sql;
