import{useEffect,useState}from'react';import{CalendarCheck,Search}from'lucide-react';import api from'../../services/api';import{Spinner,Status,Toast}from'../../components/UI';
const states=['PENDING','CONFIRMED','COMPLETED','CANCELLED'];
export default function AdminBookings(){const[rows,setRows]=useState(),[sports,setSports]=useState([]),[facilities,setFacilities]=useState([]),[filters,setFilters]=useState({search:'',date:'',sportId:'',facilityId:'',status:''}),[toast,setToast]=useState();const load=()=>api.get('/bookings',{params:filters}).then(r=>setRows(r.data));useEffect(()=>{Promise.all([api.get('/sports'),api.get('/facilities')]).then(([s,f])=>{setSports(s.data);setFacilities(f.data)});load()},[]);const setStatus=async(id,status)=>{try{await api.put(`/bookings/${id}`,{status});setToast({message:'Booking status updated.'});load()}catch(e){setToast({type:'error',message:e.response?.data?.message||'Could not update booking.'})}};if(!rows)return <Spinner/>;return <div><div className="page-head"><div><small>OPERATIONS</small><h1>Booking management</h1><p>Review every booking, filter by operating criteria and update its lifecycle.</p></div><span className="admin-pill"><CalendarCheck size={15}/> {rows.length} results</span></div><div className="toolbar"><div className="search wide-search"><Search size={16}/><input placeholder="Booking ID, user name or email" value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})} onKeyDown={e=>e.key==='Enter'&&load()}/></div><input className="filter-select" type="date" value={filters.date} onChange={e=>setFilters({...filters,date:e.target.value})}/><select className="filter-select" value={filters.sportId} onChange={e=>setFilters({...filters,sportId:e.target.value})}><option value="">All sports</option>{sports.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><select className="filter-select" value={filters.facilityId} onChange={e=>setFilters({...filters,facilityId:e.target.value})}><option value="">All facilities</option>{facilities.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select><select className="filter-select" value={filters.status} onChange={e=>setFilters({...filters,status:e.target.value})}><option value="">All statuses</option>{states.map(x=><option key={x} value={x}>{x}</option>)}</select><button className="secondary" onClick={load}>Filter</button></div><div className="panel table-wrap"><table><thead><tr><th>Booking</th><th>Member</th><th>Sport</th><th>Facility</th><th>Date / Time</th><th>Amount</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map(b=><tr key={b.id}><td><b>{b.booking_code}</b><div className="sub">#{b.id}</div></td><td><b>{b.user_name}</b><div className="sub">{b.user_email}</div></td><td>{b.sport_name}</td><td>{b.facility_name}</td><td>{b.booking_date}<div className="sub">{b.start_time}–{b.end_time}</div></td><td>₹{Number(b.amount||0).toLocaleString('en-IN')}</td><td><Status value={b.status}/></td><td><select className="mini-select" value={b.status} onChange={e=>setStatus(b.id,e.target.value)}>{states.map(x=><option key={x}>{x}</option>)}</select></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">No bookings found.</div>}</div><Toast toast={toast} onClose={()=>setToast(null)}/></div>}
