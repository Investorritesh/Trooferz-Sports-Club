import{useEffect,useState}from'react';import{BarChart3,IndianRupee,TrendingUp}from'lucide-react';import{Bar,BarChart,CartesianGrid,Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';import api from'../../services/api';import{money}from'../../utils/ui';import{Spinner}from'../../components/UI';
export default function Reports(){const[data,setData]=useState(),[error,setError]=useState('');useEffect(()=>{api.get('/dashboard/reports').then(r=>setData(r.data)).catch(e=>setError(e.response?.data?.message||'Could not load reports.'))},[]);if(error)return <div className="empty"><b>Reports unavailable</b><span>{error}</span></div>;if(!data)return <Spinner/>;const totalRevenue=data.daily.reduce((a,x)=>a+Number(x.revenue||0),0),totalBookings=data.daily.reduce((a,x)=>a+Number(x.bookings||0),0);return <div><div className="page-head"><div><small>REPORTING</small><h1>Business reports</h1><p>Daily, sport-wise, facility-wise and membership reporting from MySQL.</p></div></div><div className="stats-grid"><div className="stat-card"><small>30-day bookings</small><strong>{totalBookings}</strong><span><TrendingUp size={14}/> rolling period</span></div><div className="stat-card"><small>30-day revenue</small><strong>{money(totalRevenue)}</strong><span><IndianRupee size={14}/> confirmed + completed</span></div></div><div className="admin-chart-grid"><section className="panel chart-panel"><div className="section-head"><div><small>DAILY</small><h2>Bookings & revenue</h2></div><BarChart3 size={18}/></div><div className="chart"><ResponsiveContainer width="100%" height="100%"><LineChart data={data.daily}><CartesianGrid strokeDasharray="3 3" opacity={.15}/><XAxis dataKey="label" tickFormatter={x=>x.slice(5)}/><YAxis yAxisId="left" allowDecimals={false}/><YAxis yAxisId="right" orientation="right"/><Tooltip/><Line yAxisId="left" type="monotone" dataKey="bookings" stroke="#68d7b1" strokeWidth={2.5}/><Line yAxisId="right" type="monotone" dataKey="revenue" stroke="#78aef3" strokeWidth={2.5}/></LineChart></ResponsiveContainer></div></section><section className="panel chart-panel"><div className="section-head"><div><small>SPORT-WISE</small><h2>Demand mix</h2></div></div><div className="chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.sports.slice(0,7)}><CartesianGrid strokeDasharray="3 3" opacity={.15}/><XAxis dataKey="label"/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="bookings" fill="#78aef3" radius={[7,7,0,0]}/></BarChart></ResponsiveContainer></div></section></div><div className="admin-subgrid"><ReportTable title="Weekly summary" rows={data.weekly} extra="revenue"/><ReportTable title="Monthly summary" rows={data.monthly} extra="revenue"/></div><div className="admin-subgrid"><ReportTable title="Facility-wise" rows={data.facilities} extra="revenue"/><ReportTable title="Membership plans" rows={data.memberships} extra="memberships"/></div></div>}
function ReportTable({title,rows,extra}){return <section className="panel table-wrap"><div className="section-head"><div><small>SUMMARY</small><h2>{title}</h2></div></div><table><thead><tr><th>Label</th><th>{extra==='revenue'?'Bookings':'Members'}</th>{extra==='revenue'&&<th>Revenue</th>}</tr></thead><tbody>{rows.map(r=><tr key={r.label}><td><b>{r.label}</b></td><td>{extra==='revenue'?r.bookings:r.memberships}</td>{extra==='revenue'&&<td>{money(r.revenue)}</td>}</tr>)}</tbody></table></section>}
