import{useEffect,useState}from'react';import{ArrowUpRight,Clock3,Dumbbell}from'lucide-react';import{Link}from'react-router-dom';import api from'../services/api';import{money}from'../utils/ui';import{Spinner}from'../components/UI';
export default function Sports(){const[data,setData]=useState();useEffect(()=>{api.get('/sports').then(r=>setData(r.data.filter(s=>s.is_active)))},[]);if(!data)return <Spinner/>;return <div><PageHead eyebrow="DISCOVER" title="Sports for every pace" text="From quick doubles to full-team sessions, pick the format that fits today." action={<Link className="primary" to="/bookings">Book now <ArrowUpRight size={16}/></Link>}/><div className="catalog">{data.map(s=><article className="catalog-card" key={s.id}><div className="catalog-img"><img src={s.image_url} alt=""/><span>{s.duration_minutes} min</span></div><div className="catalog-body"><div className="round-icon"><Dumbbell size={16}/></div><h3>{s.name}</h3><p>{s.description}</p><footer><strong>{money(s.price)}</strong><small><Clock3 size={13}/> per session</small></footer></div></article>)}</div></div>}
export function PageHead({eyebrow,title,text,action}){return <div className="page-head"><div><small>{eyebrow}</small><h1>{title}</h1><p>{text}</p></div>{action}</div>}
