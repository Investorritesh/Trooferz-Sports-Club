import{useEffect,useState}from'react';import{Bell,Check,Clock3}from'lucide-react';import api from'../services/api';import{Spinner}from'../components/UI';import{PageHead}from'./Sports';
export default function Notifications(){const[rows,setRows]=useState();const load=()=>api.get('/content/notifications').then(r=>setRows(r.data));useEffect(load,[]);const read=async id=>{await api.put(`/content/notifications/${id}/read`);load()};if(!rows)return <Spinner/>;return <div><PageHead eyebrow="INBOX" title="Notifications" text="Booking, membership and club updates in one place."/><div className="notifications">{rows.map(n=><article className={`notification ${n.is_read?'read':''}`} key={n.id}><div className="notice-icon"><Bell size={17}/></div><div className="notice-copy"><b>{n.title}</b><p>{n.message}</p><small><Clock3 size={12}/> {new Date(n.created_at).toLocaleString('en-IN')}</small></div>{!n.is_read&&<button className="secondary small" onClick={()=>read(n.id)}><Check size={13}/> Read</button>}</article>)}{!rows.length&&<div className="empty"><Bell/><b>No notifications</b><span>You are all caught up.</span></div>}</div></div>}
