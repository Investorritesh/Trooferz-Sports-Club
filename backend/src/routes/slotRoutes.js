import {Router} from 'express';import * as c from '../controllers/slotController.js';import {requireAuth,requireRole} from '../middleware/auth.js';const r=Router();r.get('/',requireAuth,c.listSlots);r.post('/',requireAuth,requireRole('ADMIN'),c.createSlot);r.put('/:id',requireAuth,requireRole('ADMIN'),c.updateSlot);r.delete('/:id',requireAuth,requireRole('ADMIN'),c.deleteSlot);r.post('/block',requireAuth,requireRole('ADMIN'),c.blockSlot);export default r;
