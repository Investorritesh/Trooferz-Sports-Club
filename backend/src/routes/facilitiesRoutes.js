import {Router} from 'express';import * as c from '../controllers/facilitiesController.js';import {requireAuth,requireRole} from '../middleware/auth.js';const r=Router();r.get('/',c.listFacilities);r.get('/available',c.availableFacilities);r.post('/',requireAuth,requireRole('ADMIN'),c.createFacility);r.put('/:id',requireAuth,requireRole('ADMIN'),c.updateFacility);r.delete('/:id',requireAuth,requireRole('ADMIN'),c.deleteFacility);export default r;
