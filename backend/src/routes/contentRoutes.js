import {Router} from 'express';import * as c from '../controllers/contentController.js';import {requireAuth,requireRole} from '../middleware/auth.js';const r=Router();r.get('/public',c.publicContent);r.get('/notifications',requireAuth,requireRole('USER'),c.notifications);r.put('/notifications/:id/read',requireAuth,requireRole('USER'),c.markNotification);r.get('/announcements',requireAuth,requireRole('ADMIN'),c.listAnnouncements);r.post('/announcements',requireAuth,requireRole('ADMIN'),c.createAnnouncement);r.get('/offers',requireAuth,requireRole('ADMIN'),c.listOffers);r.post('/offers',requireAuth,requireRole('ADMIN'),c.createOffer);export default r;
