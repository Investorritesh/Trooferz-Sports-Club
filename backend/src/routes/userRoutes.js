import {Router} from 'express';import * as c from '../controllers/userController.js';import {requireAuth,requireRole} from '../middleware/auth.js';const r=Router();r.get('/',requireAuth,requireRole('ADMIN'),c.getUsers);r.get('/:id',requireAuth,requireRole('ADMIN'),c.getUser);r.put('/profile/me',requireAuth,requireRole('USER'),c.updateProfile);r.put('/:id',requireAuth,requireRole('ADMIN'),c.updateUser);export default r;
