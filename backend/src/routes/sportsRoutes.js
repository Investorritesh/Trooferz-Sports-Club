import {Router} from 'express';import * as c from '../controllers/sportsController.js';import {requireAuth,requireRole} from '../middleware/auth.js';const r=Router();r.get('/',c.listSports);r.post('/',requireAuth,requireRole('ADMIN'),c.createSport);r.put('/:id',requireAuth,requireRole('ADMIN'),c.updateSport);r.delete('/:id',requireAuth,requireRole('ADMIN'),c.deleteSport);export default r;
